# Dummy Makefile.am to trick autoreconf into installing
# config.{guess,sub} and install.sh.
#
# Our actual build definitions are in GNUmakefile.in and do
# use Autoconf, but not Automake.
#
ACLOCAL_AMFLAGS = -I auto-m4 -I m4
